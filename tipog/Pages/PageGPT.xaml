<Page x:Class="tipog.Pages.PageGPT"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:tipog.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="🧙 Помощник">
    <Grid Background="#FFDAEBFF">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ЗАГОЛОВОК -->
        <Border Grid.Row="0" Background="#FF5B9BD5" Padding="15" Margin="10">
            <TextBlock Text="🧙 ПОМОЩНИК ТИПОГРАФИИ" 
                       Foreground="White" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- ЧАТ -->
        <ScrollViewer Grid.Row="1" x:Name="ChatScroller" Margin="10">
            <StackPanel x:Name="ChatPanel"/>
        </ScrollViewer>

        <!-- ВВОД -->
        <Border Grid.Row="2" Background="#FF5B9BD5" Padding="10" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtInput" 
                         Grid.Column="0" 
                         Background="White" 
                         Foreground="Black" 
                         BorderBrush="#FF5B9BD5" 
                         BorderThickness="2" 
                         Padding="10" 
                         Height="40" 
                         KeyDown="txtInput_KeyDown"/>

                <Button x:Name="btnSend" 
                        Grid.Column="1" 
                        Content="📤" 
                        Background="#FF5B9BD5" 
                        Foreground="White" 
                        BorderBrush="White" 
                        BorderThickness="2" 
                        Width="40" 
                        Height="40" 
                        Margin="10,0,0,0" 
                        Click="btnSend_Click"/>
            </Grid>
        </Border>
    </Grid>
</Page>
